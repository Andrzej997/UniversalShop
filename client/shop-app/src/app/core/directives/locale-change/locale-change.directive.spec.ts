import {LocaleChangeDirective} from './locale-change.directive';

describe('LocaleChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new LocaleChangeDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
