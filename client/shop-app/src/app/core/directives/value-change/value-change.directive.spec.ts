import {ValueChangeDirective} from './value-change.directive';

describe('ValueChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new ValueChangeDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
